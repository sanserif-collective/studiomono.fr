---
type Props = {
  class?: string
  direction?: number
  pauseonhover?: boolean
}

const { class: className, ...rest } = Astro.props as Props
---

<marquee-carousel class:list={['flex', className]} {...rest}>
  <slot />
</marquee-carousel>
